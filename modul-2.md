<!-- Обробка подій
Щоб додати обробник подій до JSX-елементу, потрібно передати йому проп, який описує тип події. Значенням пропа повинно бути посилання на функцію зворотного виклику, яка буде викликана при настанні події.  -->
<!-- 1.Об'явимо функцію handleClick всередині компонента.
2.Задамо в середині функції логіку, наприклад, будемо використовувати alert для відображення повідомлення.
3/Додамо onClick={handleClick} до JSX-коду <button>.


const App = () => {
	const handleClick = () => {
		alert("I'm a button!");
	};

	return <button onClick={handleClick}>Click me!</button>;
};
Ми об'явили функцію handleClick і потім передали її як проп для <button>.

-Пропи подій визначаються в React та мають ім'я в стилі camelCase, починаючи з префікса on, після чого йде назва події. Наприклад: onClick, onSubmit, onChange, і так далі.
-Функції обробників подій, такі як handleClick, визначаються всередині компонентів.
-За конвенцією ім'я обробника подій починається з handle, за яким слідує назва події. Ви часто побачите щось на зразок onClick={handleClick}, onSubmit={handleSubmit}, і так далі.

Також можна визначити обробник подій безпосередньо всередині JSX як анонімну інлайн-функцію.
const App = () => {
	return <button onClick={() => alert("I'm a button!")}>Click me!</button>;
};
У багатьох випадках це нормально. Встроєні обробники подій зручні для коротких функцій. Але великі інлайн-функції в JSX можуть ускладнити читабельність розмітки компонента. -->

<!--                            Посилання на функцію
Функції, які передаються обробникам подій, повинні бути передані як посилання, а не викликані. Наприклад:
// ✅ Передача функції (правильно)
<button onClick={handleClick}>

// ❌ Виклик функції (неправильно)
<button onClick={handleClick()}>

1.У першому прикладі функція handleClick передається як обробник подій onClick. Це говорить React запам'ятати її і викликати лише тоді, коли користувач клікає кнопку.
2. У другому прикладі () в кінці handleClick() викликає функцію безпосередньо під час рендерингу, не дочекавшись події кліку.


Для інлайн функцій діють ті ж правила - якщо ви хочете визначити ваш обробник подій вбудовано, оберніть його в анонімну функцію так:
<button onClick={() => alert('You clicked me!')}>
Замість виконання коду при кожному рендерінгу, це створює функцію, яка буде викликана пізніше.
 -->

<!--                                            Об'єкт події
Callback-функція, яка передається як обробник події, незалежно від її типу, гарантовано отримає один аргумент - об'єкт події БРАУЗЕРА.

function App() {
	const handleClick = (evt) => {
		console.log(evt);
	};
	return (
		<>
			<button onClick={handleClick}>First button</button>
			<button onClick={evt => console.log(evt)}>Second button</button>
		</>
	);
};


                                                   об'єкт-обгортка SyntheticEvent
Для подій браузера у React створюється об'єкт-обгортка SyntheticEvent, призначення якої полягає в уникненні проблем, пов'язаних з кросбраузерністю. З точки зору розробника, використання SyntheticEvent не відрізняється від роботи з нативними подіями. Це дозволяє писати консистентний код для обробки подій незалежно від браузера та забезпечує зручний інтерфейс для роботи з подіями в компонентах. -->

<!-- Читання props
Оскільки обробники подій оголошуються всередині компонента, вони мають доступ до пропсів компонента.

 приклад компонента кнопки CustomButton, яка, коли на неї натискано, виводить alert зі значенням пропса message:
const CustomButton = ({ message, children }) => {
  return (
    <button onClick={() => alert(message)}>
      {children}
    </button>
  );
};

const App = () => {
  return (
    <>
      <CustomButton message="Playing music!">
        Play some music
      </CustomButton>
      <CustomButton message="Uploading your data!">
        Upload data
      </CustomButton>
    </>
  );
} -->

<!--                                        Стан компонента(state)
Компонентам часто потрібно змінювати те, що відображається на екрані внаслідок взаємодії з користувачем. Введення даних в форму повинно оновлювати поле вводу, клікання в слайдері зображень повинно змінювати зображення, натискання "купити" повинно додавати товар у кошик.

СТАН КОМПОНЕНТА (state) - це його внутрішні динамічні дані, у React стан - такий вид пам'яті, що специфічний для компонента. Компонентам потрібно "запам'ятовувати" різні речі: поточне значення введення, поточне зображення, кошик покупок.
-Пропси (props) - це дані, які передаються компоненту ззовні,
-а стан (state) - це його внутрішні динамічні дані.

Коли стан або пропси компонента ЗМІНЮЮТЬСЯ, відбувається ОНОВЛЕННЯ та ПОВТОРНИЙ РЕНДЕР JSX. Іншими словами, стан - це реактивні дані, які контролюють те, що відображається в інтерфейсі.

-У стані зберігається мінімально необхідний набір даних, на основі яких можна обчислити все необхідне для рендеру інтерфейсу.
-У стані може зберігатися будь-який тип даних.
-Стан може змінюватися відповідно до дій користувача.
-Під час зміни стану компонент повертає оновлену розмітку і змінює інтерфейс.


Зміна стану компонента ніколи не впливає на його батьків чи сусідів, а тільки на його дочірні елементи.
 💌Це називається ОДНОСПРЯМОВАНИЙ ПОТІК ДАНИХ - модель передачі даних вниз по дереву компонентів.
 -->
<!--                                             REACT'S DOCUMENTATION -->
<!-- 💌у React є три типи вхідних даних, які ви можете читати під час рендеру:
-props ,
-state ,
-context. Ви завжди повинні розглядати ці вхідні дані як доступні лише для читання. -->

<!-- 💌Реакт і ф-ціональне програмування дотримується концепції ЧИСТИХ Ф-ЦІЙ:
-- ф-ція не змінює жодних об’єктів або змінних, які існували до її виклику.
-- З урахуванням однакових вхідних даних чиста функція завжди повинна повертати той самий результат.-->

<!--💌 React пропонує «строгий режим», у якому він ВИКЛИКАЄ ФУНКЦІЮ кожного компонента ДВІЧІ під час розробки. Двічі викликаючи функції компонента, строгий режим допомагає знайти компоненти, які порушують  правила чистих функцій. -->

<!-- Чисті функції не змінюють змінні за межами області видимості функції або об’єкти, які були створені до виклику — це робить їх нечистими!
Однак цілком НОРМАЛЬНО ЗМІНЮВАТИ змінні та об’єкти, які ви щойно СТВОРИЛИ ПІД ЧАС РЕНДЕРУ(ВІЗУАЛІЗАЦІЇ). Це називається «локальною мутацією». У цьому прикладі ви створюєте []масив, призначаєте його змінній cups, а потім pushдодаєте в нього десяток чашок:
function Cup({ guest }) {
  return <h2>Tea cup for guest #{guest}</h2>;
}

export default function TeaGathering() {
  let cups = [];
  for (let i = 1; i <= 12; i++) {
    cups.push(<Cup key={i} guest={i} />);
  }
  return cups;
} -->
<!--
 Ці зміни — оновлення екрана, запуск анімації, зміна даних — називаються ПОБІЧНИМИ ЕФЕКТАМИ. Це речі, які відбуваються «збоку» , а не під час рендеру.

У React побічні ефекти зазвичай НАЛЕЖАТЬ ДО ОБРОБНИКІВ ПОДІЙ. Обробники подій — це функції, які React запускає, коли ви виконуєте певну дію — наприклад, коли ви натискаєте кнопку. Незважаючи на те, що ОБРОБНИКИ ПОДІЙ визначені у вашому компоненті, вони НЕ ЗАПУСКАЮТЬСЯ ПІД ЧАС РЕНДЕРУ! Тому обробники подій НЕ ПОВИННІ БУТИ ЧИСТИМИ.

Якщо ви вичерпали всі інші варіанти та не можете знайти потрібний обробник подій для свого побічного ефекту, ви все одно можете приєднати його до повернутого JSX за допомогою useEffectвиклику у вашому компоненті. Це вказує React виконати його пізніше, після рендерингу, коли дозволені побічні ефекти. Однак цей підхід має бути останнім заходом.

Якщо це можливо, спробуйте виразити свою логіку лише за допомогою відтворення. Ви будете здивовані, як далеко це може вас завести! -->
<!--
 -->
<!--                                                  Реактивність -->
<!--  Кожне натискання на кнопку повинно змінити значення змінної і відобразити нове значення в інтерфейсі. Проте це не працюватиме
const App = () => {
	let clicks = 0;

  const handleClick = () => {
    clicks = clicks + 1;
  };

	return <button onClick={handleClick}>Current: {clicks}</button>
};

Обробник подій handleClick оновлює локальну змінну clicks. Проте дві речі заважають змінам бути видимими:
1.Локальні змінні не зберігаються між рендерами. Коли React рендерить цей компонент вдруге, він рендерить його з нуля — не враховує жодних змін у локальних змінних.
2.Зміни локальних змінних не викликають повторних рендерів. React не усвідомлює необхідності повторного рендерингу компонента з новими даними.


Для оновлення компонента новими даними потрібно виконати дві речі:
1.Зберегти дані між рендерами.
2.Заставити React рендерити компонент з новими даними (повторний рендеринг).

Перепишемо код з використанням стану:
1.Щоб додати змінну стану, імпортуйте ХУК useState з React угорі файлу компонента.
import { useState } from 'react';
2.замінимо локальну змінну clicks на синтаксис [ clicks, setClicks ] - це деструктуризація масиву. Масив, який повертає useState, завжди має точно два елементи: перший (clicks) - це змінна стану, а другий (setClicks) - це функція для її встановлення.
3.У handleClick використовуємо setClicks для зміни стану clicks.
import { useState } from 'react';

const App = () => {
	const [ clicks, setClicks ] = useState(0);

  const handleClick = () => {
    // clicks = clicks + 1;
		setClicks(clicks + 1);
  };

	return <button onClick={handleClick}>Current: {clicks}</button>
};
Тепер, при кліку на кнопку, стан в змінній clicks буде змінюватися, що змусить компонент оновлюватися і повторно рендерити JSX з оновленими значеннями.
 -->

<!--                                                   Хук HOC (Higher-Order Components) -->
<!-- Хуки - спеціальні функції, що починаються з use, за допомогою яких можна "підключитися" до різних можливостей React. Стан - це лише одна з їх можливостей, але ви зустрінете інші хуки пізніше.

Хуки оновлюються АСИНХРОННО()

Якщо ви бажаєте використовувати хук, імпортуйте його в файлі компонента з бібліотеки React.
import { useState } from 'react';



Коли ви викликаєте useState, ви повідомляєте React, що бажаєте, щоб цей компонент запам'ятовував щось. У цьому випадку ми хочемо, щоб React запам'ятовував clicks.
const [clicks, setClicks] = useState(0);



Єдиним аргументом для useState є початкове значення змінної стану. У цьому прикладі початкове значення clicks встановлено на 0 за допомогою useState(0)

Кожного разу, коли компонент рендериться, useState повертає масив, що містить два значення:
1.Змінну стану (clicks) зі збереженим поточним значенням.
2.Функцію встановлення стану (setClicks), яка може оновлювати змінну стану і викликати повторний рендеринг компонента.


import { useState } from 'react';

const App = () => {
	const [clicks, setClicks] = useState(0);

  const handleClick = () => {
		setClicks(clicks + 1);
  };

	return <button onClick={handleClick}>Current: {clicks}</button>
};



Використовуючи деструктуризацію, можна задати будь-які імена змінних. Рекомендовано називати цю пару як [something, setSomething]. Ви можете називати їх будь-як, але конвенції полегшують розуміння коду в різних проектах.


Ось як це відбувається на практиці:

const [clicks, setClicks] = useState(0);
1. Компонент рендериться вперше. Тому що ми передали 0 в useState як початкове значення для clicks, нам повертається [0, setClicks]. React запам'ятовує, що 0 - це останнє значення стану.
2.Оновлюємо стан. Коли користувач натискає кнопку, викликається setClicks(clicks + 1). На даний момент clicks - це 0, отже setClicks(clicks + 1) отримується setClicks(1). Це повідомляє React, що тепер clicks - це 1, і викликає ще один рендеринг.
3.Другий рендеринг вашого компонента. React все ще бачить useState(0), але через те, що React пам'ятає, що ви встановили clicks в 1, воно повертає [1, setClicks] замість того.
І так далі!
 -->

 <!--                                                    Декілька станів
Ви можете мати стільки змінних стану, скільки потрібно в одному компоненті. Для кожного використовуйте окремий useState.
const [a, setA] = useState();
const [b, setB] = useState();
const [c, setC] = useState();

Додамо до компонента App ще один стан isOpen, який буде контролювати відображення абзацу тексту.
function App() {
  const [clicks, setClicks] = useState(0);
  const [isOpen, setIsOpen] = useState(false);

  const handleClick = () => {
    setClicks(clicks + 1);
  };

  const handleToggle = () => {
    setIsOpen(!isOpen);
  };

  return (
    <>
      <button onClick={handleClick}>Current: {clicks}</button>
      <button onClick={handleToggle}>{isOpen ? "Hide" : "Show"}</button>
      {isOpen && <p>Now you can see me!</p>}
    </>
  );
};

Кожен зі станів clicks та isOpen незалежний і оновлюється своїми відповідними функціями встановлення setClicks та setIsOpen відповідно.
 -->

 <!--                                       Ізоляція стану
Стан є локальним для екземпляра компонента. Якщо ми відображаємо один і той самий компонент декілька разів, кожна копія матиме абсолютно ізольований стан! Зміна одного з них не вплине на інший.

Створимо компонент ClickCounter, в якому винесемо стан кількості кліків та розмітку кнопки.
const ClickCounter = () => {
	const [clicks, setClicks] = useState(0);

	const handleClick = () => {
    setClicks(clicks + 1);
  };
	
	return <button onClick={handleClick}>Current: {clicks}</button>
};


Відобразимо дві копії компонента ClickCounter в App.
const App = () => {
  return (
    <>
			<ClickCounter />
			<ClickCounter />
    </>
  );
};

При кліку на кнопки ви побачите, що значення кількості кліків, інакше кажучи, їхні стани, є незалежними і оновлюються окремо одне від одного.

Це робить стан відмінним від звичайних змінних. Стан є локальним для конкретної копії компонента. Ми відобразили два компоненти <ClickCounter />, тому їхні стани зберігаються окремо.

Зверніть увагу на те, як компонент App нічого не знає про стан ClickCounter. На відміну від props, стан є повністю приватним для компонента, що його визначає. Батьківський компонент не може його змінити. Це дозволяє додавати стан до будь-якого компонента чи видаляти його без впливу на решту компонентів. -->

<!--                                         патерн "підняття стану"-->
<!-- щоб змінити стан батька під час події в дитині  -->
<!--Треба щоб всі ClickCounter тримали свої стани синхронізованими? Правильний спосіб зробити це в React - це видалити стан з дочірніх компонентів і додати його до їхнього найближчого спільного батька, тобто СТВОРИТИ СПІЛЬНИЙ СТАН.

1.Винесемо стан clicks в компонент App.
2. використаємо пропси для передачі необхідних даних від батька до дитини, тобто з App до ClickCounter.
Передамо ЗНАЧЕННЯ СТАНУ як пропс value та МЕТОД ЗМІНИ СТАНУ як пропс onUpdate.
const ClickCounter = ({ value, onUpdate }) => {
	return <button onClick={onUpdate}>Current: {value}</button>
};

const App = () => {
	const [clicks, setClicks] = useState(0);

	// Функція, яку будемо передавати в ClickCounter
	// для виклику під час кліку
	const handleClick = () => {
    setClicks(clicks + 1);
  };

  return (
    <>
			<ClickCounter value={clicks} onUpdate={handleClick} />
			<ClickCounter value={clicks} onUpdate={handleClick} />
    </>
  );
};
Тепер при кліку на будь-якій з кнопок ClickCounter стан компонента App змінюється, після чого він оновлюється, і нові пропси передаються його дочірнім компонентам, які також оновлюються.

ОТЖЕ
-У батька є стан і метод, який його змінює.
-Дочірньому елементу у формі пропу передається метод батька, що змінює стан батька.
-У дочірньому елементі відбувається виклик переданого йому методу. Під час виклику цього методу змінюється стан батька.
-Відбувається рендер піддерева компонентів батька.
-Патерн підняття стану може мати будь-яку вкладеність і залежить від складності дерева компонентів.-->

<!--                                          Оновлення об'єктів
Стан може містити будь-який тип значення, включаючи об'єкти.
const App = () => {
  const [values, setValues] = useState({
    x: 0,
    y: 0,
	  });

	const updateX = () => {
			setValues({
			...values,
			x: values.x + 1
		});
	};

	const updateY = () => {
		setValues({
			...values,
			y: values.y + 1
		});
	};

  return (
    <div>
      <p>
        x: {values.x}, y: {values.y}
      </p>

      <button onClick={updateX}>Update x</button>
      <button onClick={updateY}>Update y</button>
    </div>
  );
};

--слід розглядати будь-який об'єкт в стані лише для читання. Змінювати його не можна!
Об'єкти, як і будь-який інший стан, ОНОВЛЮЮТЬСЯ ЗА ДОП Ф-ЦІЇ СЕТТЕРА.
--коли потрібно оновити об'єкт, слід ЗРОБИТИ КОПІЮ існуючого ключа
 const updateX = () => {
	setValues({
		...values,
		x: values.x + 1
	});
};
інакше ви втратите властивість y в об'єкті стану values. Справа в тому, що функція-сеттер записує отримане значення поверх існуючого стану, і замість {x: 1, y: 0} у стані буде об'єкт {x: 1}.
-->

<!-- ХУКИ ЛИШЕ НА ВЕРХНЬОМУ РІВНІ
хуки можна викликати лише на верхньому рівні функції компонента. Це означає, що вони повинні знаходитися безпосередньо в тілі функції компонента, а не всередині циклів, умов, вкладених функцій і так далі. Такі обмеження спрощують написання логіки компонента та роблять код більш читабельним.
const App = () => {
	// ❌ Буде помилка
  if (isLoggedIn) {
    const [username, setUsername] = useState("");
  }

	// ❌ Буде помилка
	for(let i = 0; i < 10; i++) {
		const [value, setValue] = useState(0);
	}

  // ...
};

// ✅ Так правильно
const App = () => {
  const [username, setUsername] = useState("");
	const [value, setValue] = useState(0);
};
 -->

<!-- Віртуальний DOM
При оголошенні React-компонентів і написанні JSX фактично взаємодіємо не з DOM безпосередньо. Існує проміжний шар між кодом компонентів і DOM -  віртуальний DOM.

Браузерний DOM - це структура об'єктів, що представляє HTML-документ у браузері. При завантаженні HTML-сторінки браузер створює DOM-структуру, яка відображає структуру HTML-коду. Це дозволяє JavaScript взаємодіяти з кожним елементом на сторінці, змінювати їх стилі, властивості, додавати чи видаляти елементи і т. д.
З кожною зміною DOM браузер виконує кілька трудомістких операцій. Часті операції оновлення такого дерева негативно впливають на продуктивність та реакцію інтерфейсу. Тому він повільний, і оновлення його потрібно робити ефективно.


Віртуальний DOM - це абстракція, легка копія реального DOM-дерева у вигляді JSON-документа. Це своєрідний проміжний етап між React-компонентами і фактичним DOM у браузері.

-ІСНУЄ ТІЛЬКИ В ПАМ*ЯТІ і не рендериться в браузері
-Не залежить від внутрішньої реалізації браузера
-Використовує найкращі практики оновлення реального DOM
-ЗБИРАЄ ЛОНОВЛЕННЯ В ГРУППИ для оптимізації рендеру


Алгоритм оновлення DOM
У React кожен елемент інтерфейсу - це компонент (власний чи вбудований), який залежить від пропсів або стану і представлений вузлами віртуального DOM-дерева. Взаємодія користувача з інтерфейсом змінює стан застосунку.
1. При зміні компонента створюється нове віртуальне DOM-дерево, яке представляє оновлений стан інтерфейсу користувача.
2.React порівнює (diffing) два віртуальних DOM-дерева - до та після оновлення, і визначає, які саме частини реального DOM потрібно змінити.
3.Застосовується оптимальний варіант внесення змін до реального DOM. Це гарантує, що в реальному DOM-дереві провадиться мінімальна кількість оновлень, тим самим підвищуючи продуктивність інтерфейсу.
 -->
